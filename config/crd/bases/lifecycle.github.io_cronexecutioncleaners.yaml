---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: cronexecutioncleaners.lifecycle.github.io
spec:
  group: lifecycle.github.io
  names:
    kind: CronExecutionCleaner
    listKind: CronExecutionCleanerList
    plural: cronexecutioncleaners
    singular: cronexecutioncleaner
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CronExecutionCleaner is the Schema for the cronexecutioncleaners
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CronExecutionCleanerSpec defines the desired state of CronExecutionCleaner
            properties:
              cleanupStuck:
                description: Configuration for cleaning stuck Jobs
                properties:
                  enabled:
                    description: Whether stuck job cleanup is enabled
                    type: boolean
                  stuckAfter:
                    description: Duration after which a running Job is considered
                      stuck
                    type: string
                required:
                - enabled
                - stuckAfter
                type: object
              cronJobName:
                description: Name of the CronJob whose executions should be cleaned
                type: string
              foo:
                description: Foo is an example field of CronExecutionCleaner. Edit
                  cronexecutioncleaner_types.go to remove/update
                type: string
              namespace:
                description: Namespace in which the target CronJob exists
                type: string
              retain:
                description: Retention policy for completed Jobs
                properties:
                  failedJobs:
                    description: Number of failed Jobs to retain
                    type: integer
                  successfulJobs:
                    description: Number of successful Jobs to retain
                    type: integer
                required:
                - failedJobs
                - successfulJobs
                type: object
              runInterval:
                description: Interval at which cleanup logic runs
                type: string
            required:
            - cleanupStuck
            - cronJobName
            - namespace
            - retain
            - runInterval
            type: object
          status:
            description: CronExecutionCleanerStatus defines the observed state of
              CronExecutionCleaner
            properties:
              jobsDeleted:
                description: Total number of Jobs deleted
                type: integer
              lastRunTime:
                description: Last time the cleanup ran
                format: date-time
                type: string
              podsDeleted:
                description: Total number of Pods deleted
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
